
let name = "John"
let hasSubscription = true;  // true means the user has an active subscription
let hasCompletedKYC = true; 

if(hasSubscription && hasCompletedKYC)
    console.log("KYC Verification Complete . You are granted access to premium features !!")
else
    console.log("KYC Incomplete!!");