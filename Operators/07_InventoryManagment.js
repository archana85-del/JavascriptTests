
        let inventory = 500;
        let inventorySold = 100
        let remainingInventory = inventory- inventorySold
        let restocked = 50
        let updatedInventory = remainingInventory + restocked;
        console.log(updatedInventory);