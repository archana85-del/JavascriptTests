

          let userRole = "Admin";
        let isActive = true; // true means the user is active

        if(userRole == "Admin" && isActive){
            console.log("You have full access")
        }
        else
            console.log("You dont have access!!")